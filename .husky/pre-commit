#!/bin/sh
# TypeScript check — zero type errors before committing
echo "→ TypeScript check..."
npx tsc --noEmit
if [ $? -ne 0 ]; then
  echo "✗ TypeScript errors found. Fix them before committing."
  exit 1
fi
echo "✓ All checks passed."
